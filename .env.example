# Copie este arquivo para .env e ajuste com as credenciais do seu MySQL.
# O arquivo .env NÃO deve ser versionado (já está no .gitignore).

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=library_management
DB_USER=root
DB_PASS=
